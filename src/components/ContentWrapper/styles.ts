import styled from "styled-components";
import { background, navbarHeight } from "../../styles/global"

export const Container = styled.div`
  background: ${background};
  padding-top: ${navbarHeight};
  margin-top: 38px;
  padding-left: 5%;
  padding-right: 5%;
  padding-bottom: 5%;
  height: 100%;
`